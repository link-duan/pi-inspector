{
  "name": "pi-inspector",
  "version": "0.1.2",
  "description": "Inspect the current pi session: live system prompt and full transcript dashboard in the browser",
  "keywords": [
    "debug",
    "extension",
    "inspect",
    "pi",
    "pi-coding-agent",
    "pi-package"
  ],
  "homepage": "https://github.com/link-duan/pi-inspector#readme",
  "bugs": {
    "url": "https://github.com/link-duan/pi-inspector/issues"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/link-duan/pi-inspector.git"
  },
  "files": [
    "src/**/*.ts",
    "src/**/*.tsx",
    "src/**/*.html",
    "src/**/*.css",
    "src/web/dist/**",
    "README.md"
  ],
  "type": "module",
  "scripts": {
    "build": "bun run build:web",
    "build:web": "bun build src/web/src/index.tsx --outdir src/web/dist --target=browser --minify",
    "check": "bun run build:web && bun run typecheck && bun run lint && bun run format:check",
    "dev:web": "bun build src/web/src/index.tsx --outdir src/web/dist --target=browser --watch",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "lint": "oxlint src",
    "lint:fix": "oxlint --fix src",
    "typecheck": "tsc"
  },
  "dependencies": {
    "highlight.js": "^11.12.0",
    "lucide-react": "^1.41.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.84.3",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.7",
    "lefthook": "^2.1.12",
    "oxfmt": "^0.66.0",
    "oxlint": "^1.81.0",
    "typescript": "^5.6.0"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "pi": {
    "extensions": [
      "./src/index.ts"
    ],
    "image": "https://raw.githubusercontent.com/link-duan/pi-inspector/main/assets/pi-inspect-example.png"
  }
}
